<template>
  <view class="app-container">
    <!-- 接码邮箱区域 -->
    <view class="card">
      <text class="card-title">接码邮箱 - {{timer}}</text>
      <view class="email-display">
        <text class="email-text">{{ state.emailAddress }}</text>
      </view>
      <view class="button-group">
        <button class="neo-button" @click="copyText(state.emailAddress)">点击复制邮箱</button>
        <button class="neo-button" @click="generateSub">随机邮箱</button>
        <button class="neo-button" @click="generateSubAndCopy">随机邮箱并复制</button>
      </view>
    </view>

    <!-- 验证码区域 -->
    <view class="card" v-if="state.emailData.message_not_read">
      <text class="card-title">实时邮件</text>
      <view class="code-box" id="code-text">
        <text class="code-text" style="word-break: break-all">{{ state.emailData.message_not_read.code }}</text>
      </view>
      <view class="info-box">
        <text style="word-break: break-all">
          {{ cheerioLoad(state.emailData.message_not_read.content).text().replace(/\s+/g, ' ').trim() }}
        </text>
      </view>
      <button v-if="state.emailData.message_not_read.code" class="neo-button"
              @click="copyText(state.emailData.message_not_read.code)">点击复制验证码
      </button>
    </view>

  </view>
</template>


<script setup>
import {reactive,ref} from 'vue'
import {onLoad, onShow, onReady, onUnload} from '@dcloudio/uni-app'
import {index, generate} from "../../api";
import {load as cheerioLoad} from 'cheerio'

const state = reactive({
  emailAddress: '',
  emailData: {}
})

const storeEmail = {
  set(email) {
    uni.setStorageSync('email', email)
  },
  get() {
    return uni.getStorageSync('email')
  },
  remove() {
    uni.removeStorageSync('email')
  }
}

const loadEmail = () => {
  return index({email: storeEmail.get()}).then(res => {
    if (res.code === 1) {
      state.emailData = res.data
      if (res.data.address) {
        state.emailAddress = res.data.address
        storeEmail.set(res.data.address)

      }
    }
    // console.log(res)
  })
}

let timer = ref();

const initTaskLoadEmail = () => {
  const task = () => {
    timer.value = setTimeout(async () => {
      await loadEmail()
      task()
    }, 2000)
  }

  task();
}

const copyText = (text) => {
  uni.setClipboardData({
    data: text,
    success: () => {
      uni.showToast({
        title: '复制成功',
        icon: 'success'
      });
    },
    fail: () => {
      uni.showToast({
        title: '复制失败',
        icon: 'none'
      });
    }
  });
}


const generateSub = () => {

  return new Promise((resolve, reject) => {

    const handle = () => {
      uni.showLoading()
      generate().then(res => {
        if (res.code === 1) {
          uni.showToast({title: '生成成功'})
          storeEmail.set(res.data.email)
          loadEmail()
          resolve(res.data.email)
        } else {
          reject()
          uni.showToast({title: res.msg, icon: 'none'})
        }
      }).finally(() => {
        uni.hideLoading()
      })
    }

    if (state.emailData.message_not_read) {
      handle()
      return
    }
    uni.showModal({
      content: '你当前没有收到邮件，确认要重新生成邮箱？',
      title: '确认操作', success: (res) => {

        if (res.confirm) {
          handle()
        }

      }
    })
  })
}

const generateSubAndCopy = () => {
  generateSub().then(email => {
    copyText(email)
  })
}




onLoad(async () => {
  await loadEmail()
  initTaskLoadEmail();

})
onShow(async () => {


})


</script>

<style lang="scss">
.app-container {
  min-height: 100vh;
  background: linear-gradient(to bottom right, #0f2027, #203a43, #2c5364);
  padding: 60rpx 30rpx;
  display: flex;
  flex-direction: column;
  gap: 60rpx;
}

.card {
  background: rgba(255, 255, 255, 0.05);
  backdrop-filter: blur(20rpx);
  border-radius: 30rpx;
  box-shadow: 0 8rpx 20rpx rgba(0, 0, 0, 0.3);
  padding: 40rpx 30rpx;
  color: #fff;
}

.card-title {
  font-size: 40rpx;
  font-weight: bold;
  margin-bottom: 30rpx;
  color: #00ffff;
  letter-spacing: 2rpx;
}

.email-display {
  margin-bottom: 40rpx;
  padding: 30rpx;
  border: 2rpx dashed #00ffff;
  border-radius: 20rpx;
  text-align: center;
  background-color: rgba(0, 255, 255, 0.1);
}

.email-text {
  font-size: 36rpx;
  color: #00ffff;
  word-break: break-all;
}

.button-group {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
}

.neo-button {
  background: linear-gradient(to right, #00c6ff, #0072ff);
  color: white;
  font-weight: bold;
  font-size: 30rpx;
  padding: 20rpx;
  border: none;
  border-radius: 20rpx;
  text-align: center;
  box-shadow: 0 6rpx 12rpx rgba(0, 114, 255, 0.4);
  transition: transform 0.2s ease;
}

.neo-button:active {
  transform: scale(0.95);
  box-shadow: none;
}

.code-box {
  font-size: 72rpx;
  font-weight: 800;
  text-align: center;
  color: #ffffff;
  margin-bottom: 30rpx;
  //letter-spacing: 6rpx;
}

.info-box {
  background-color: rgba(255, 255, 255, 0.08);
  border-radius: 20rpx;
  padding: 30rpx;
  text-align: center;
  font-size: 28rpx;
  color: #e0e0e0;
  margin-bottom: 30rpx;
}


.text-box {
  margin-bottom: 40rpx;
  padding: 40rpx 0;
  display: flex;
  min-height: 300rpx;
  background-color: #eee;
  justify-content: center;
  align-items: center;
  text-align: center;
  font-size: 30rpx;
  color: #353535;
  line-height: 1.8;
}

.image {
  width: 25px;
  height: 25px;
}

.text {
  font-size: 14px;
  margin-top: 5px;
}

.example-body {
  /* #ifndef APP-NVUE */
  // display: block;
  /* #endif */
}

.grid-dynamic-box {
  margin-bottom: 15px;
}

.grid-item-box {
  flex: 1;
  // position: relative;
  /* #ifndef APP-NVUE */
  display: flex;
  /* #endif */
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 15px 0;
}

.grid-item-box-row {
  flex: 1;
  // position: relative;
  /* #ifndef APP-NVUE */
  display: flex;
  /* #endif */
  flex-direction: row;
  align-items: center;
  justify-content: center;
  padding: 15px 0;
}

.grid-dot {
  position: absolute;
  top: 5px;
  right: 15px;
}

.swiper {
  height: 420px;
}

/* #ifdef H5 */
@media screen and (min-width: 768px) and (max-width: 1425px) {
  .swiper {
    height: 630px;
  }
}

@media screen and (min-width: 1425px) {
  .swiper {
    height: 830px;
  }
}

/* #endif */
</style>
